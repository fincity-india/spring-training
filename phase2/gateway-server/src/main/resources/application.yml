server:
  port: 8080

spring:
  config:
    import: "configserver:"

  application:
    name: api-gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: false

    config:
      uri: http://localhost:8888

eureka:
  client:
    enabled: false
