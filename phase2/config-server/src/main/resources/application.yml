server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Basavaraj9972/config-repo
          default-label: main
          search-paths: /
      label: main

logging:
  level:
    org.springframework.cloud.config: DEBUG
