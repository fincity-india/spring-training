server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/imthiyaz348/new-config-repo.git
  application:
    name: config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true